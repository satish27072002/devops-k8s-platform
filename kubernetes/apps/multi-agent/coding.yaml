apiVersion: apps/v1
kind: Deployment
metadata:
  name: multi-agent-coding
  namespace: multi-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: multi-agent-coding
  template:
    metadata:
      labels:
        app: multi-agent-coding
    spec:
      containers:
        - name: coding
          image: satish27072002/multi-agent-sdlc-coding:ef7b34142c5a
          command:
            - python
            - -m
            - src.server.agent_runner
          ports:
            - containerPort: 9001
          env:
            - name: GROQ_API_KEY
              valueFrom:
                secretKeyRef:
                  name: multi-agent-secrets
                  key: GROQ_API_KEY
            - name: AGENT_NAME
              value: coding
            - name: AGENT_PORT
              value: "9001"
          readinessProbe:
            httpGet:
              path: /health
              port: 9001
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 9001
            initialDelaySeconds: 15
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: multi-agent-coding
  namespace: multi-agent
spec:
  selector:
    app: multi-agent-coding
  ports:
    - name: http
      port: 9001
      targetPort: 9001
